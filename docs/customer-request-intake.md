# Phase 1 identity and request intake

## Verified integration baseline

PR [#1](https://github.com/Rmdn96/Naql365/pull/1) targeted develop, had an independent APPROVED review by Rmdn696 on accepted SHA `57d32d146e4d18ddbde372db9b85bfe420f78825`, both required checks successful, and GitHub mergeability `clean`. Administrator-enforced review, last-push approval and strict checks remained enabled. A protected rebase merge produced develop `388c6953b6759657e0748de1aae7c8b2b41bb19e`. Its tree exactly matched the accepted SHA; original Phase 0 `184ac23` remained an ancestor. The Phase 1 branch was created from fetched origin/develop and pushed with a clean tree before changes.

## Schema-gap analysis before migration work

Inspected all three applied migrations, generated public types, RLS helpers/policies, SSR/callback, storage signing, and existing tests. Existing tables are relational skeletons, not an implemented request workflow. This inventory describes requirements, not claims of completed implementation.

| Requirement          | A: already supported                                                                                           | B: additive migration/work required                                                                                                                                                  | C: deliberately deferred                                  |
| -------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| Identity             | Auth user/profile trigger; display_name, locale                                                                | Profile phone; validated profile mutation; confirmed-user onboarding transaction                                                                                                     | Demographics, staff self-registration                     |
| Tenant/customer      | Organizations, typed active/suspended memberships, customers, CUSTOMER role                                    | Trusted enrollment configuration, idempotent customer membership enrollment                                                                                                          | SaaS tenant billing, customer-created staff organizations |
| Services             | Tenant-scoped code and UUID; service_areas FK                                                                  | Active flag, AR/EN labels, property relevance, narrowly scoped customer catalogue read                                                                                               | Pricing, geographic availability engine                   |
| Requests             | UUID, organization_id, customer_id, timestamps, composite FK                                                   | DRAFT/SUBMITTED/CANCELLED, optimistic revision, creation idempotency key, selected service, description/notes, preferred local date/window, contact snapshot, submitted_at/reference | Quotes/orders/trips and dispatch states                   |
| Route/property       | No address or property columns                                                                                 | Two normalized request locations: pickup/delivery, city/district/details/notes, optional floor/elevator/access                                                                       | Coordinates, maps, distance, multi-stop customer wizard   |
| Items                | UUID, tenant and request FK                                                                                    | Description, positive bounded quantity, notes, ordering                                                                                                                              | Price/weight estimation                                   |
| Additional services  | No requirement catalogue                                                                                       | Tenant-scoped options and request selections, normalized FKs                                                                                                                         | Charges or price rules                                    |
| Attachments          | Generated organization/profile/UUID paths, file registry, request linkage, private buckets and signed download | Bounded image metadata and upload reservation/finalization/removal lifecycle; draft-only association                                                                                 | PDF initially, malware scanning (no scanner claim)        |
| Reference/submission | No existing reference/state                                                                                    | Monthly Riyadh counter, atomic locked submit, immutable payload, audit event                                                                                                         | Acceptance, order creation                                |
| Autosave             | No writes open                                                                                                 | Bounded typed RPC payload, expected revision, same retry returns current result; conflicts never silently overwrite                                                                  | Offline authoritative localStorage                        |
| Audit/analytics      | Protected audit table, actor/tenant fields                                                                     | Minimal identity/request/file events and payload-free request_started/request_completed metadata                                                                                     | Vendor analytics, customer payload telemetry              |

## Decisions

Use the existing operating-organization membership model. There is no pre-existing personal-organization lifecycle to reuse. A private operator-managed singleton enrollment setting selects the operating organization; public callers cannot choose it or any role. Onboarding derives auth.uid(), requires a confirmed identity, serializes retries per profile, creates only a customer membership/customer record and the fixed CUSTOMER grant, and never reactivates suspension. No email or editable metadata grants authority. Configure an isolated Staging organization and catalogues through a guarded operator setup, not customer data seeds or manual schema patches.

Core request data stays relational. A JSON RPC envelope is transport only; addresses, items and selections are validated and written to normalized tables. Draft writes lock the request, verify ownership and active membership, and compare a monotonic revision. The server returns a conflict on stale writes. Repeating the last mutation identifier returns its result without duplicating it. Submission validates the persisted complete draft and acquires a monthly counter with INSERT ON CONFLICT UPDATE, never MAX()+1. Retrying submission returns the already assigned reference. Submitted payloads are immutable to customers.

Phase 1 uploads accept JPEG/PNG/WebP images only, at most 3 MiB each and 8 files per draft. Reserve generated paths in a locked draft transaction, validate bytes and MIME server-side, upload privately using the user's session, then finalize association. Incomplete reservations are not submitted attachments. Removal is a retryable two-phase lifecycle: revoke read visibility, remove the Storage object, then remove the registry association. Cancellation marks pending cleanup; cleanup never removes schema/catalogues. No malware scanning is claimed.

Phase 2+ remains excluded: pricing, quotes, orders, dispatch, driver workflows, GPS, payments, messaging automation, AI, marketplace and billing.

## Implemented customer lifecycle

`/[locale]/register` uses Supabase email/password registration with confirmation and an exact PKCE callback. Confirmation establishes a session; it does not grant staff access. `account` displays onboarding for a confirmed new identity, an editable customer profile for an active enrolled customer, or an unavailable/forbidden state for suspended/ineligible participation. Password recovery uses the same origin's locale-specific `/password` callback target. All identity forms use generic localized errors and server-selected redirects.

`/[locale]/request/[id]` presents eight steps: service, route, shipment/items/images, property, extras, schedule, contact and review. The API creates persisted drafts; no customer/organization identifiers are accepted from the browser. Saves debounce for 750 ms, serialize in-flight changes, retain a mutation identifier for network retries and return a visible conflict on stale revision. Internal navigation flushes pending changes; browser unload warns when unsaved. Refresh resumes persisted content, not client-local authoritative data. Successful submit navigates to the immutable detail page. My Requests paginates twenty rows and includes resumable drafts and submitted/cancelled records.

Customers may cancel a draft. Attachments enter removing state and the UI supports retrying byte/registry cleanup. A cancelled request cannot be edited or submitted. An already-issued download remains a bearer capability until its sixty-second expiry; suspension blocks new access immediately. The download handler is authenticated and does not expose public bucket URLs.

## Reproduction and operational configuration

Apply repository migrations in order, regenerate types, then run `npm run staging:configure-intake` with the explicit guarded Staging project/organization identifiers and authenticated Supabase CLI. This idempotent operator command establishes the operating organization, private enrollment setting and small active AR/EN service/extra catalogues. These are configuration, not fabricated customer transactions. End users cannot select the enrollment organization. Production setup is outside this phase.

Use the exact deployed Preview origin in the Staging Auth Site URL and four explicit redirect entries: `/auth/callback?locale=ar`, `/auth/callback?locale=en` and each locale with `&next=/{locale}/password`. Rotate these together with a new Preview; never wildcard arbitrary destinations. Preview receives only the independent Staging Supabase public URL/key; no app admin key is used.

Run `npm run test:staging:intake` after configuration with the protected Preview origin and operator-only bypass credential. The harness generates disposable identities in memory, exercises AR desktop/EN mobile, tests direct HTTP authorization and removes only its own generated data. Run it sequentially with the foundation hosted suites. The report records executed results; the procedure itself is not evidence of PASS.

## Provider references

- [Supabase password/SSR authentication](https://supabase.com/docs/guides/auth/passwords)
- [PKCE session flow](https://supabase.com/docs/guides/auth/sessions/pkce-flow)
- [Storage access control](https://supabase.com/docs/guides/storage/security/access-control)
- [Standard uploads](https://supabase.com/docs/guides/storage/uploads/standard-uploads)
